server:
  port: 9001

spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #  Nacos地址

# 暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
